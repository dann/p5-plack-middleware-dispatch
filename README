This is Perl module Plack::Middleware::Dispatch.

INSTALLATION

Plack::Middleware::Dispatch installation is straightforward. If your CPAN shell is set up,
you should just be able to do

    % cpan Plack::Middleware::Dispatch

Download it, unpack it, then build it as per the usual:

    % perl Makefile.PL
    % make && make test

Then install it:

    % make install

DOCUMENTATION

Plack::Middleware::Dispatch documentation is available as in POD. So you can do:

    % perldoc Plack::Middleware::Dispatch

to read the documentation online with your favorite pager.

Takatoshi Kitano
